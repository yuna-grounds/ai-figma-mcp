# ai-figma

Claude Code(VS Code)에서 Figma를 직접 조작하기 위한 로컬 MCP 작업 공간.

## MCP 접속 방법

1. Figma에서 "Talk to Figma MCP" 플러그인 설치
2. Bun 설치 (`bunx` 포함 — `.mcp.json`에 `TalkToFigma` 서버로 등록되어 있음)
3. 터미널에서 릴레이 서버 먼저 실행: `cursor-talk-to-figma-mcp` 폴더에서 `bun run src/socket.ts`
4. 릴레이 서버가 켜진 상태에서 Figma 플러그인 실행
5. 플러그인이 서버 연결에 성공하면 채널명이 표시됨 — 복사
6. Claude Code에서 해당 채널명으로 `join_channel` 접속 시도

릴레이 서버는 터미널을 닫으면 같이 종료되므로 재접속할 때마다 3번부터 다시 실행한다.

## 작업 규칙

- 커밋/푸시는 사용자가 Git Bash에서 직접 실행한다. Claude는 파일 내용만 수정한다.
- 변경 내용 요약 및 세부사항 정리는 Claude가 이 README에 작성/갱신한다.
- Claude는 README를 수정한 뒤 변경 내용을 요약해서 보여주고 확인을 받는다. 커밋/푸시는 그 확인 이후 사용자가 별도로 진행한다.
- Figma 접근 권한은 AI 전용으로 지정된 파일에만 한정한다.
- 새로운 디자인 작업은 항상 새 프레임에 만든다. 기존 프레임은 수정하지 않고 그대로 두어, 사용자가 디자인 안을 비교하고 최종 판단할 수 있게 한다.
- Figma 작업은 무조건 "ai" 페이지 안에서만 한다. Figma 파일에는 사용자의 실제 작업 페이지(예: UXUI_250822)가 별도로 존재하므로, 매 작업 세션 시작 시 `get_document_info`로 현재 페이지 이름이 "ai"인지 반드시 먼저 확인한다. "ai"가 아니면 작업을 진행하지 않고 사용자에게 페이지 전환을 요청한다.
  - 실전 팁: 최상위 프레임을 새로 만들 때 `create_frame`의 `parentId`에 "ai" 페이지 ID(`449:18655`)를 직접 지정하면, 사용자가 다른 페이지 탭을 보고 있어도 항상 "ai" 페이지에 정확히 생성된다 (2026-07-14 확인).

## 변경 로그

| 날짜 | 영역 | 내용 |
|---|---|---|
| 07-13 | 워크스페이스 | TalkToFigma MCP 워크스페이스 구성, 웹소켓 릴레이 localhost 전용 바인딩으로 보안 수정 |
| 07-13 | 표지 | 3번 프레임을 임팩트 있는 프로젝트 표지로 재구성 (4번 프레임 신규, 기존 3번 보존) |
| 07-13 | P1 세트 | 리얼타임메디체크 프로젝트를 의료진·환자 양방향 스토리로 재구성, 실제 데이터 기반 P1-01~10 신규 제작 |
| 07-14 | 보강 프레임 14~19 | 케이스 스터디 분석 후 리서치 방법론·Define 우선순위·전체 IA·가설검증 매트릭스·핵심지표 정의·기여도 프레임 6개 추가 |
| 07-14 | P1 세트 재구성 | 슬라이드 순서 변경, Define 우선순위 페이지 삭제, 핵심개선 3종 실제 스크린샷 적용, 유저저니맵 전면 재구성, 서비스 흐름 페이지 실제 플로우차트로 교체 |
| 07-14 | 비주얼 보강 | 리서치·문제정의 페이지에 벤다이어그램, 결과지표 페이지에 막대그래프·체크리스트 추가 |
| 07-14 | P1-02/03/04 재구성 | 레퍼런스 레이아웃 기반 리서치 인사이트·솔루션 가설·저니맵+인사이트 슬라이드 신규 제작 (기존 프레임 보존) |
| 07-14 | 텍스트 위계·패딩 | 제목/소제목/본문/본문설명 4단계 크기 위계 통일, 프레임 100px 여백 고정 |
| 07-14 | 타이틀·레이아웃 표준화 | 질문형 타이틀 + 본문설명 + 하단 정렬 레이아웃을 P1 세트 전체 표준 규칙으로 적용 |
| 07-15 | 2번째 프로젝트 착수 | 재고관리 대시보드 UI 리뉴얼 프로젝트 시작 (표지 "250934") |
| 07-15 | 2-01 플로우 다이어그램 | 손그림 플로우차트를 정리된 다이어그램으로 재구성 |
| 07-15 | 5-1/5-2 구조 분석 | 기획 문서(스펙 주석 포함)와 순수 디자인 프레임 구분·분석 |
| 07-15 | 2-02 인사이트 슬라이드 | 분석 기반 인사이트 4가지를 카드로 정리 (가상 데이터) |

## 구성

- `.mcp.json` — Claude Code에 등록된 MCP 서버 설정 (`TalkToFigma`)
- `cursor-talk-to-figma-mcp/` — [grab/cursor-talk-to-figma-mcp](https://github.com/grab/cursor-talk-to-figma-mcp) 클론. 웹소켓 릴레이 + Figma 데스크톱 플러그인으로 구성되며, Figma 공식 REST API/MCP 서버를 거치지 않고 Plugin API를 직접 호출한다. 요금제별 호출 제한과 무관하게 동작.
- `claude-talk-to-figma-mcp/` — [arinspunk/claude-talk-to-figma-mcp](https://github.com/arinspunk/claude-talk-to-figma-mcp) 클론, 참고용 대체 구현.
- `patches/` — 로컬 보안 수정 diff.

두 서브폴더는 각자 자체 `.git`을 가진 클론이라 이 레포에는 내용이 아니라 "이 시점의 커밋" 포인터만 기록된다 (embedded repo).
