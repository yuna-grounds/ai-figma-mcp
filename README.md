# ai-figma

Claude Code(VS Code)에서 Figma를 직접 조작하기 위한 로컬 MCP 작업 공간.

## 작업 규칙

- 커밋/푸시는 사용자가 Git Bash에서 직접 실행한다. Claude는 파일 내용만 수정한다.
- 변경 내용 요약 및 세부사항 정리는 Claude가 이 README에 작성/갱신한다.
- Claude는 README를 수정한 뒤 변경 내용을 요약해서 보여주고 확인을 받는다. 커밋/푸시는 그 확인 이후 사용자가 별도로 진행한다.
- Figma 접근 권한은 AI 전용으로 지정된 파일에만 한정한다.
- 새로운 디자인 작업은 항상 새 프레임에 만든다. 기존 프레임은 수정하지 않고 그대로 두어, 사용자가 디자인 안을 비교하고 최종 판단할 수 있게 한다.
- Figma 작업은 무조건 "ai" 페이지 안에서만 한다. Figma 파일에는 사용자의 실제 작업 페이지(예: UXUI_250822)가 별도로 존재하므로, 매 작업 세션 시작 시 `get_document_info`로 현재 페이지 이름이 "ai"인지 반드시 먼저 확인한다. "ai"가 아니면 작업을 진행하지 않고 사용자에게 페이지 전환을 요청한다.
  - 실전 팁: 최상위 프레임을 새로 만들 때 `create_frame`의 `parentId`에 "ai" 페이지 ID(`449:18655`)를 직접 지정하면, 사용자가 다른 페이지 탭을 보고 있어도 항상 "ai" 페이지에 정확히 생성된다 (2026-07-14 확인).

## 변경 로그

### 2026-07-13
- TalkToFigma MCP 워크스페이스 구성 (`.mcp.json`, Bun 설치)
- 웹소켓 릴레이 외부 네트워크 노출 차단 — localhost 전용 바인딩으로 변경 (`patches/socket-localhost-binding.patch`)
- Figma "ai" 파일: 3번 프레임(리얼타임메디체크 프로젝트 화면)을 임팩트 있는 프로젝트 표지로 재구성한 안을 4번 프레임에 새로 생성 (기존 3번 프레임은 그대로 보존)
- Figma "ai" 파일: 리얼타임메디체크 프로젝트를 "의료진·환자 양방향 관점" 스토리로 재구성, 실제 데이터 기반 10장 세트(P1-01~P1-10) 새로 생성 — 표지 / 리서치 방법론 / 두 포지션 문제 정의 / 유저 저니맵 / Define 우선순위 / 핵심개선 3종(홈화면·알림톡·AI분석, 기존 7·9·11페이지 참고해 재구성) / 결과지표 / 회고·기여도·다음 프로젝트 예고. 14~19번(가상 데이터 초안)은 그대로 두고 보존

### 2026-07-14
- Figma "ai" 파일: 리얼타임메디체크 케이스 스터디(4~13번 프레임) UXUI 실무자 관점 분석 후, 보강 프레임 6개를 새로 생성 (14~19번, 가상 데이터로 내용 채움 — 실제 수치·인원은 검증 필요)
  - 14: 리서치 방법론 (인터뷰·FGD·설문·벤치마킹)
  - 15: Define 우선순위 근거 (Impact-Effort 매트릭스)
  - 16: 전체 서비스 IA(사이트맵)
  - 17: 가설-검증 매트릭스 (개선안별 검증 방법과 결과 지표 연결)
  - 18: 핵심 지표 3종의 조작적 정의·측정 기간·표본
  - 19: 단계별 기여도(Discover~Deliver) 및 협업 방식
- Figma "ai" 파일: 사용자 피드백 반영해 P1 세트(리얼타임메디체크) 대대적 수정
  - 순서 변경: 표지 → 리서치 → 유저저니맵 → 두 포지션 문제 정의 → 전체 서비스 흐름(IA/플로우) → 핵심개선 3종 → 결과지표 → 회고
  - Define 우선순위 페이지(P1-05) 삭제, 검증 방법 내용은 핵심개선 3페이지 상단 코멘트 태그로 이전
  - 핵심개선 3페이지: 첨부받은 실제 ASIS 스크린샷·TOBE 컴포넌트·레이더차트 이미지로 간이 목업 교체, 라벨/위치 정리
  - 유저저니맵(P1-04) 전면 재구성: 페르소나 요약 2종 + 3단계 그룹 헤더 + 단계·행동·위치·소요시간·반응감정 5행 × 9단계 컬럼 + 감정 흐름 계단형 그래프. 연결 끊김 중 중복 생성된 텍스트 정리
  - 전체 서비스 흐름 페이지(구 16번 IA): 첨부받은 실제 Data Flow Chart(Draw.io) + User Flow Chart(Figjam) 이미지로 재구성, 핵심개선 페이지 앞으로 이동
- Figma "ai" 파일: P1 세트 비주얼 밀도 보강 (텍스트 위주 페이지에 차트/벤다이어그램 추가)
  - 리서치 방법론(P1-02): 카드 아래 3원 벤다이어그램 추가 — 만족도조사·인터뷰·설문조사의 교차 검증 시각화
  - 두 포지션 문제 정의(P1-03): 2원 벤다이어그램 추가 — 의료진·환자 공통 문제(QR 스캔)를 교집합으로 표현
  - 결과 지표(P1-09): 서비스 이용률·고객 만족도 카드에 적용 전/후 막대그래프 추가, 에러 페이지 개선 카드에 점검 항목 체크리스트 추가
  - P1-04 유저저니맵 텍스트 중복(연결 끊김 잔재) 정리
- Figma "ai" 파일: 사용자 피드백("각 장표가 전달하려는 바가 불명확") 반영해 핵심 3장표 레퍼런스 기반으로 재구성 (기존 P1-02/03/04는 보존, 새 프레임으로 제작)
  - **P1-02 문제 정의(리서치 인사이트)** — Behance 레퍼런스 레이아웃 참고: 3가지 리서치 방법 카드가 하단 인사이트 박스로 수렴하는 구조. 핵심 발견("의료진 업무 부담 ↑ · 환자 서비스 이용 목표 ↓")을 명시적으로 드러냄
  - **P1-04 유저저니맵&인사이트** — 기존 P1-04를 복제해 긍정/부정 감정 곡선 영역 삭제, 하단에 메인 인사이트("가장 힘든 순간은 접종 초반에 몰려있다") 박스 추가
  - **P1-03 포지션별 솔루션 가설** — Behance 레퍼런스 레이아웃 참고: Pain Point → Needs → Solution 4단 구조, 핵심 UX에 해당하는 솔루션에는 배지 표시
  - 작업 중 Figma 파일에 사용자의 실제 작업 페이지(UXUI_250822)가 별도로 있다는 것을 발견 — 거기 실수로 생성된 중복 프레임 정리, "ai 페이지 전용" 규칙을 위 작업 규칙에 추가
- Figma "ai" 파일: 위 3장표(P1-02/P1-04/P1-03) 텍스트 위계·크기·패딩 정리 (기존 프레임 그대로 두고 같은 프레임 내용만 삭제 후 재구성)
  - 제목(44~52px) → 소제목(24px) → 본문(22~26px) → 본문 설명(16~18px) 4단계 크기 위계로 통일, 전반적으로 텍스트 크기 확대
  - 전체 프레임(1920×1080)에 상하좌우 100px 여백을 고정하고 그 안에서만 요소가 배치되도록 좌표 재계산
  - 서술형 문장을 모두 존댓말(-습니다체)로 통일 (인용부호 안 말풍선은 원래 화법 유지)
  - 유저저니맵 표 안에서 텍스트 확대 시 칸이 겹치는 문제 발견 — "위치 상관없음" 표현을 "위치 무관" 등으로 축약해 해결

## 구성

- `.mcp.json` — Claude Code에 등록된 MCP 서버 설정 (`TalkToFigma`)
- `cursor-talk-to-figma-mcp/` — [grab/cursor-talk-to-figma-mcp](https://github.com/grab/cursor-talk-to-figma-mcp) 클론. 웹소켓 릴레이 + Figma 데스크톱 플러그인으로 구성되며, Figma 공식 REST API/MCP 서버를 거치지 않고 Plugin API를 직접 호출한다. 요금제별 호출 제한과 무관하게 동작.
- `claude-talk-to-figma-mcp/` — [arinspunk/claude-talk-to-figma-mcp](https://github.com/arinspunk/claude-talk-to-figma-mcp) 클론, 참고용 대체 구현.
- `patches/` — 로컬 보안 수정 diff.

두 서브폴더는 각자 자체 `.git`을 가진 클론이라 이 레포에는 내용이 아니라 "이 시점의 커밋" 포인터만 기록된다 (embedded repo).

## 워크플로

1. VS Code에서 이 폴더를 열면 `.mcp.json`이 로드되어 `TalkToFigma` MCP 서버가 뜬다.
2. `cursor-talk-to-figma-mcp` 안에서 웹소켓 릴레이 실행: `bun run src/socket.ts` (재부팅/터미널 종료 시 같이 죽으므로 매번 다시 켜야 함).
3. Figma 데스크톱 앱에서 작업 파일을 열고 플러그인을 실행 → 채널 접속.
4. Claude Code 쪽에서 `join_channel`로 같은 채널에 접속.
5. 이후 MCP 도구로 작업.
